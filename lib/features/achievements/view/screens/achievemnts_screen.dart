import 'package:flutter/material.dart';

class AchievemntsScreen extends StatelessWidget {
  const AchievemntsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Achievements screen"),),
    );
  }
}
